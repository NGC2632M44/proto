{
  "mcpServers": {
    "proto": {
      "type": "local",
      "command": "python",
      "args": ["${workspaceFolder}/scripts/mcp_proto.py"],
      "env": {
        "PROTO_STORE": "${env:HOME}/.protocols"
      }
    }
  }
}